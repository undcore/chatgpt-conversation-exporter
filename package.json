{
  "name": "chatgpt-conversation-exporter",
  "version": "0.2.0",
  "description": "Export the active ChatGPT conversation branch to Markdown from your browser.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node scripts/build-bookmarklet.mjs",
    "test": "node --test",
    "check": "npm run build && node --check src/chatgpt-export-to-markdown.js && node --check scripts/build-bookmarklet.mjs && npm test && git diff --check && git diff --exit-code -- dist/chatgpt-export-to-markdown.bookmarklet.txt"
  }
}
